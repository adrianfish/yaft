<a href="/portal/tool/{{placementId}}/forums" onClick="return yaft.switchState('forums');">{{translate 'forums_label'}}</a>-&gt;<a href="{{currentForum.url}}" onClick="return yaft.switchState('forum',{'forumId':'{{currentForum.id}}'});">{{currentForum.title}}</a>-&gt;<a href="{{currentDiscussion.url}}" onClick="return yaft.switchState('full',{'discussionId':'{{currentDiscussion.id}}'});">{{currentDiscussion.subject}}</a>-&gt;{{translate 'authors_label'}}
